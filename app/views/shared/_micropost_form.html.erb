<%= form_for(@micropost) do |f| %>

    <!--
    form_for(@micropost, remote: true) do |f| %>
        -->

    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
    <%= f.text_area :content, placeholder: "Utwórz nową wiadomość..." %>
  </div>
    <%= f.submit "Wyślij", class: "btn btn-large btn-primary" %>
<% end %>